create table USER_FEELCYCLE (
    ID int not null auto_increment,
    USER_PASS_FEELCYCLE varchar(64),
    USER_ID_FEELCYCLE varchar(64),
    USER_ID varchar(64),
    constraint USER_FEELCYCLE_PK primary key(ID)
);
